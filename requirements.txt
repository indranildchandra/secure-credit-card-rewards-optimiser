# Google Agent Development Kit (ADK)
# Target runtime: Python 3.12. The version markers below keep older Pythons
# working, but 3.12 -> google-adk 1.33.0 is the developed/tested combination.
google-adk==1.33.0; python_version>="3.10"
google-adk==1.15.1; python_version>="3.9" and python_version<"3.10"
google-adk==0.3.0; python_version<"3.9"

# Env file loading
python-dotenv==1.2.2

# Local model support (Ollama via LiteLLM)
litellm==1.83.14

# Free web search for the live offers/devaluation check (no API key)
ddgs==9.14.2

# Testing (offline unit tests for the deterministic tools)
pytest==9.0.3
pytest-asyncio==1.3.0

# Linting / formatting (dev)
ruff==0.15.20
black==26.5.1
